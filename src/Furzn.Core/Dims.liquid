namespace Furzn.Core


    

[<Interface>]
type IDim<'Self when IDim<'Self>> =
    abstract member Dim: int


{% for dim in dims %}
    {% if dim != 'X' %}    
[<Struct>]
type D{{dim}} =
    interface IDim<D{{dim}}> with
        member _.Dim = {{dim}}
    {% endif %}
{% endfor %}


[<Struct>]
type DX(dim: int) =
    interface IDim<DX> with
        member _.Dim = dim
